spring:
  application:
    name: ScheduleManagement_Upgrade
  datasource:
    url: jdbc:mysql://localhost:3306/schedule
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true